# 部署配置示例
# 复制此文件为 .env 并填写实际值

# 服务器 SSH 连接信息
SERVER_USER=user
SERVER_IP=your-server-ip-here
REMOTE_PATH=/var/www/typing-games/backend

# 应用配置
APP_NAME=typing-games-backend
NODE_ENV=production

# 服务器端口（可选，默认3000）
PORT=3000
